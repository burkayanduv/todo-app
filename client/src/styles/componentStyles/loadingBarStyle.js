const loadingBarStyle = {
    loadingBarWrapper: {
        marginTop: "auto",
        marginBottom: "40px",
        display: "flex",
        justifyContent: "center"
    }
}

export default loadingBarStyle